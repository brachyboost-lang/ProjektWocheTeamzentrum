<Window x:Class="ProjektWocheTeamzentrum.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjektWocheTeamzentrum"
        mc:Ignorable="d"
        Title="Teamzentrum" Height="800" Width="1200">
    <StackPanel>
        <DockPanel>
            <Grid>
                    <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Events" Command="{Binding ShowEventsCommand}" Margin="10"/>
                        <Button Grid.Column="1" Content="Usermanagment" Command="{Binding ShowUsermanagementCommand}" Margin="10"/>
                        <Button Grid.Column="2" Content="Coaching" Command="{Binding ShowCoachingCommand}" Margin="10"/>
                    <Button Grid.Column="4" Content="Settings" Command="{Binding ShowSettingsCommand}" Margin="10"/>
                
            </Grid>
        </DockPanel>
        <ContentControl x:Name="MainContentControl" Margin="10" />
    </StackPanel> 
</Window>
